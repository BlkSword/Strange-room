# Strange Room

让物理上同处一室的人，像把设备摊在同一张桌子上一样共享文件。

局域网直连、扫码即连、不留痕。没有账号，没有云端，传完就结束。

> **状态**：v1 内核与命令行客户端已完成并验证；桌面界面（Tauri）开发中。
> 本文只描述**已经能用**的能力，未实现的部分见文末「已知限制」。

## 它解决什么

同一间会议室里，你想把 2GB 的素材给旁边的同事。现有做法都不顺：
微信传会压缩且有大小限制、U 盘要拔插、网盘要先上传再下载（数据还留在了云上）、
AirDrop 只认苹果设备。

Strange Room 的做法是：你在自己电脑上开一个房间，屏幕上出现二维码；
对方扫一下就连上了，文件走局域网直传，速度是网盘量级之上的事。

## 快速开始

需要 Rust 工具链（1.75+）。

```bash
cargo build --release
```

**分享文件的一方**（在主机的屏幕上）：

```bash
# 会打印二维码，等待对方扫码接收
./target/release/sr send ./photos

# 也可以用 --no-qr 只打印连接串，适合远程终端/脚本
./target/release/sr send ./photos --no-qr
```

**接收文件的一方**：

```bash
./target/release/sr receive "srx1:......" --to ./downloads
```

连接串由发送端打印出来，也可以从二维码扫出来。

## 已经验证的能力

| 能力 | 说明 |
|---|---|
| 文件与文件夹传输 | 保留目录结构；相对路径经安全校验，不接受的路径会被拒绝 |
| 完整性校验 | 每个文件传完用 BLAKE3 校验，不匹配就不落成正式文件 |
| 断点续传 | 中断后重连，只补传缺少的部分；本地进度带**前缀哈希**，状态不可信时整文件重传 |
| 指纹校验 | 连接时只接受证书指纹与二维码一致的主机，不一致立刻失败 |
| 大文件 | 流式读写，不把文件读进内存；接收前预分配空间，磁盘不足提前失败 |
| 不留痕 | 全部成功后会清掉本地的临时状态文件；不联网、不上传任何数据 |

实测：局域网内约 30–55 MB/s；600MB 文件在传输中被强杀后重连，只补传了缺的 452MB。

## 工作原理

**一次扫码同时传递三件事**：主机地址、会话 ID、以及主机证书的指纹。
这让"怎么找到对方"和"怎么信任对方"用同一个动作解决——不需要 CA，
不需要账号，也不需要云端参与。信任锚点通过屏幕（带外信道）传递，
安全性等价于 Signal 的安全码比对。

传输用 QUIC（`quinn`），自带加密与多路复用；v1 用单条双向流跑完整会话。
协议层与传输解耦，将来换传输不影响上层。

## 已知限制

这些是**现在还没有**的，不要期待：

- **没有图形界面**。目前只有命令行客户端（`sr`）。桌面端在做。
- **没有自动发现设备**。必须扫码或粘贴连接串，不会自动列出同一 WiFi 下的设备。
- **没有公网能力**。仅限同一局域网；跨网段、AP 隔离、企业防火墙会连不上。
- **没有移动端**。
- 仅支持同版本客户端互连（协议版本不匹配会明确报错）。

## 开发

```bash
cargo test              # 全部测试：内核单元测试 + 端到端传输 + 断点续传
cargo build --release
```

测试里包含真实起两个 QUIC 端点互传文件的端到端用例，以及"中断后重连只补差额"、
"不可信检查点必须整文件重传"、"指纹不符必须秒级失败"这类回归测试。

## 许可证

MIT
