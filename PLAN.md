# Strange Room 重写工程规划

> **状态**：已定稿，进入实现
> **日期**：2026-09-10
> **前置**：`PRODUCT-DECISION.md`（产品方向决策，不再重复论证，本文只讲怎么落地）
> **原则**：先让最窄的一条链路端到端跑通，再谈任何其他东西

---

## 一、已锁定的决策

以下几条已经拍板，实现期间不再讨论，除非出现新的硬约束：

| # | 决策 | 说明 |
|---|---|---|
| D1 | **全原生、全重写** | 不为任何存量代码做兼容；`src/`、`server/`、`electron/` 仅作参考，不迁移 |
| D2 | **Rust 内核独立于 UI** | `core` 不依赖 Tauri，UI 可推倒重做十次而内核不动 |
| D3 | **v1 访客也装同一个客户端** | 不做浏览器版。理由：浏览器版会逼出双客户端，成本翻倍且绕不开安全上下文问题。二维码里同时放"可读地址 + 原生跳转参数"作为扫码失败时的降级路径 |
| D4 | **v1 传输用 QUIC** | 多路复用 + 加密内建 + 丢包表现好；协议层做成传输无关，便于后续替换 |
| D5 | **v1 不做 mDNS** | 二维码里已经含地址，发现不是 v1 的必要条件。mDNS 放到 v1.5，因为它不可靠时会伤害首次体验 |
| D6 | **v1 只做一件事：传文件** | 剪贴板、笔记、白板、代码编辑、屏幕共享全部推迟。见第四章 |
| D7 | **UI 放在最后** | 内核 CLI 跑通 → 最小可用 UI → 设计系统与视觉打磨。顺序不可颠倒 |

---

## 二、仓库结构

```
share/                    # 新项目根，与旧代码完全隔离
  Cargo.toml              # workspace
  .cargo/config.toml      # 镜像源（去掉失效代理）
  crates/
    core/                 # 纯 Rust 内核：不依赖任何 UI
      src/
        lib.rs
        config.rs         # 会话配置、默认值、落盘路径
        crypto.rs         # 会话密钥、房间密钥、加解密
        qr.rs             # 二维码载荷编解码
        transfer/         # 传输引擎
          mod.rs
          chunker.rs      # 分块、偏移、断点状态
          receiver.rs     # 落盘、预分配、原子改名
          sender.rs       # 流式读取、限流、进度
          resume.rs       # 断点状态持久化
        net/
          mod.rs
          protocol.rs     # 帧格式、消息定义、编解码
          quic.rs         # quinn 传输实现
          tls.rs          # 自签证书 + 指纹
        fs_util.rs        # 预分配、原子改名、路径安全
        progress.rs       # 进度模型（UI 无关）
      tests/              # 端到端测试（两个进程互相传）
    cli/                  # 无界面客户端，v1 的主验证工具
    desktop/              # Tauri v2 壳（后期）
```

**为什么 `core` 必须独立**：文件传输、协议、断点续传的正确性只能用测试证明，不能靠"打开界面点一下"。CLI 让最难的部分可以在没有 UI 的情况下被反复验证。

---

## 三、v1 验收标准

沿用产品文档那条，但补上可测量的判定方式：

> **一个非技术用户，60 秒内把一个 2GB 的文件夹传给同房间的人，全程无需任何解释。**

工程侧拆解为可测的指标：

| 指标 | 目标 | 验证方式 |
|---|---|---|
| 首次可用时间 | 装好后 ≤ 10 秒出现二维码 | 手动计时 |
| 同 WiFi 吞吐 | ≥ 60 MB/s（千兆/AC 环境） | `cli` 传 2GB 计时 |
| 断点续传 | 中途 kill 掉接收端，重启后从断点继续，不重传已完成的块 | 自动化测试 |
| 完整性 | 传输后 BLAKE3 与源文件一致 | 自动化测试 |
| 磁盘满 | 提前失败并给出可操作提示，不留半个文件 | 自动化测试（限制写入量） |
| 路径安全 | 恶意路径（`../`、绝对路径、Windows 保留名）不能写到目标目录外 | 单元测试 |

---

## 四、v1 范围（明确到"做什么 / 不做什么"）

**做：**

1. 主机端：启动一个会话，生成二维码，等待连接
2. 接收端：扫描/输入二维码内容，连上主机
3. 文件传输：单文件 + 文件夹（保留相对路径），带进度、速度、剩余时间
4. 断点续传：接收端崩溃或断连后，重启可从断点继续
5. 完整性校验：每个文件传完做 BLAKE3 校验
6. 失败可操作的提示（磁盘满、连接断、路径非法）

**不做（v1 明确的非目标）：**

- ❌ mDNS / 自动发现（v1.5）
- ❌ 共享剪贴板、共享笔记（v1.5+）
- ❌ 白板、代码编辑、聊天
- ❌ 屏幕共享、音视频（v2）
- ❌ 公网中继、账号、云存储
- ❌ 移动端（内核留好接口，但 v1 不交付）

---

## 五、协议设计（v1 核心）

### 5.1 传输层

QUIC（`quinn`）+ 自签证书。每台机器启动时生成一张长期自签证书，指纹固定写入二维码。**首次连接靠二维码里的指纹做带外校验**，等价于 Signal 的安全码比对。

### 5.2 一条流 = 一个文件

QUIC 的多路复用价值在这里：多个文件各自一条流，互不阻塞。v1 先串行发送（一次一个文件），协议上支持并行。

### 5.3 握手与请求流程

```
接收端                                    发送端（主机）
   |------ ClientHello(version, room_id) ------>|
   |<----- ServerHello(version, session_id) ----|
   |------ FileOffer(file_id, name, size,        |
   |        path, blake3, dst_offset) --------->|   # dst_offset>0 即续传
   |<----- OfferAck(file_id, start_offset) -----|
   |<========= 数据块（frame 流式）=============|
   |------ FileDone(file_id, blake3) ---------->|
```

**关键点：续传是"接收端主导"的。** 接收端先扫自己本地已收了多少，通过 `dst_offset` 告诉发送端从哪开始，发送端回 `start_offset` 确认。这样即使接收端崩溃多次也能继续。

### 5.4 帧格式

```
+--------+--------+------------------+
| type   | len    | payload          |   1 byte + 4 bytes(LE u32) + payload
+--------+--------+------------------+
```

单帧上限 4 MiB，数据块默认 1 MiB。长度前缀 + 显式类型，便于将来加帧类型而不破坏兼容。

---

## 六、文件传输的实现约束（v1 工作量真正所在）

这部分是最容易翻车的地方，逐条落实：

1. **绝不把整个文件读进内存**：发送端 `read` → 加密 → 写流；接收端 流 → 写盘，全程固定大小缓冲。
2. **接收端必须先预分配磁盘空间**。不同平台语义不同（`fallocate` / `F_FULLFSYNC` / `SetFileValidData`），磁盘满要在**开始传之前**就失败。
3. **先写 `.part` 临时文件，校验通过后再原子改名**。用户永远看不到半个文件。
4. **断点状态要持久化到磁盘**，不是只存一个进度数字。接收目录里有 `resume.json`，记录每个文件的 `bytes_received` 和校验状态。
5. **路径安全必须先做**：拒绝绝对路径、`..`、Windows 保留名（CON/PRN/NUL...）、非法字符，全部限定在目标目录内。
6. **背压**：网络收得比磁盘写得快时不能无限缓冲（靠 `await` 写入天然背压）。
7. **文件夹保留相对路径**，并在发送端就归一化好，避免接收端解析出危险路径。

---

## 七、阶段与里程碑

| 阶段 | 内容 | 交付物 | 预估 |
|---|---|---|---|
| **M0** | 骨架、配置、协议定义、帧编解码 | `core` 可编译 + 协议单元测试 | 2 天 |
| **M1** | QUIC 握手 + 证书指纹校验 | 两个进程能建立可信连接 | 3 天 |
| **M2** | 单文件传输 + BLAKE3 校验 | `cli send/receive` 能传通一个文件 | 4 天 |
| **M3** | 断点续传 + 预分配 + 原子改名 + 路径安全 | 自动化测试覆盖中断恢复 | 4 天 |
| **M4** | 文件夹、多文件、进度显示 | 能传 2GB 文件夹并看到实时进度 | 3 天 |
| **M5** | 二维码生成/解析、会话生命周期 | `cli host` 打印二维码，另一台扫码加入 | 2 天 |
| **M6** | 最小可用 UI（Tauri） | 3 个界面：主窗口/传输中/结果 | 5 天 |
| **M7** | 设计系统与视觉打磨 | 可交付的 v1 | 5 天 |

**总计约 4 周**（不含移动端、mDNS、其他功能）。CLI 阶段的产物是长期的测试资产，不是一次性脚手架。

---

## 八、测试策略

- **单元测试**：帧编解码、二维码载荷、路径安全、分块偏移计算
- **集成测试**：`core/tests/` 里起真实 QUIC 端点互传，含中断恢复、大文件、非法路径
- **永不回归**：从第一次提交起挂 `cargo test`，不再重蹈"8800 行零测试"
- **手动验收**：M4 和 M7 结束后各做一次真机互传

---

## 九、风险与对策

| 风险 | 对策 |
|---|---|
| QUIC 在部分企业 WiFi / 防火墙被拦 | 协议层做传输无关；v1.5 准备 TCP 回退路径；内建一键网络诊断 |
| Windows 首次监听触发防火墙弹窗 | 在 UI 里提前解释，并给出手动放行指引 |
| Rust 学习曲线拖慢进度 | 内核先做 CLI，用 `cargo test` 快速反馈；不追求一开始就优雅 |
| 范围蔓延 | 第四章"不做清单"是对策；任何新增功能必须先改第四章 |
| 单人开发无人 review | 用集成测试替代部分 review 作用；关键协议变更写进 ADR |

---

# 附：实现状态（更新于 2026-09-12）

> 本文件前半部分是**规划**；这一节是**实际做出来的东西**。
> 计划与实现不一致的地方，以这一节为准。

## 已完成：M0 – M5（内核 + CLI）

```
share/
  crates/core          # 内核：不依赖任何 UI，CLI / Tauri / 移动端都能复用
    qr.rs              # 二维码载荷：一次扫码同时传递 地址 + 会话 + 证书指纹
    identity.rs        # 自签身份，单文件承载、原子发布
    protocol.rs        # 与传输无关的帧协议（1B 类型 + 4B 长度 + 载荷）
    net/quic.rs        # QUIC 传输 + 会话驱动（发送端 / 接收端）
    net/tls.rs         # 只认指纹的 TLS 校验器
    transfer/plan.rs   # 清单规划（递归目录、BLAKE3、稳定文件 ID）
    transfer/resume.rs # 断点续传状态（含前缀哈希校验）
    fs_util.rs         # 路径安全、空间预留、原子改名
  crates/cli           # sr send / sr receive —— v1 的主验证工具
```

**实测结果**（两台进程、真实 QUIC）：

| 场景 | 结果 |
|---|---|
| 文件夹传输（含中文路径、嵌套目录） | 逐字节一致，目录结构保留 |
| 600 MB 单文件 | 局域网约 30–55 MB/s |
| 传一半强杀接收端 → 重连续传 | 只补传缺少的部分，最终字节一致 |
| 指纹不匹配（二维码被换过） | **秒级失败**并明确提示，而非等到超时 |
| 过期会话 ID | 被拒绝，且目标目录不落盘任何文件 |
| 传输成功后 | 无 `.part`、无 `resume.json` 残留 |

**测试**：`cargo test` 共 69 个用例（内核 59 + 端到端 5 + 续传 2 + CLI 3），零编译警告。
断点续传、指纹校验、路径安全这些关键路径都有专门的回归测试守着。

## 实现过程中发现并修掉的真问题

这些大多**只有真机互传才会暴露**，列在这里是因为它们是这个产品最容易踩的坑：

1. **路径穿越**：`/etc/passwd` 被 `split('/')` 洗成了相对路径 `etc/passwd`。
   绝对路径必须在切分**之前**拦掉。已加回归测试。
2. **预分配让"长度"失去意义**：接收端会把 `.part` 预分配成完整大小，于是
   「磁盘长度 ≥ 记录长度」永远成立，不可信检查点会被误信。判据改为
   **前缀内容哈希**——长度够不代表内容是我们要的那一段。
3. **检查点 fsync 拖死连接**：在异步任务里做 `fsync`，慢盘/杀毒软件下可能
   阻塞几十秒，久到 QUIC 空闲超时把连接判死，表现为"传到一半莫名断开"。
   检查点只是优化，正确性由最终校验保证，因此去掉 fsync。
4. **收尾互相等待**：双方都 `await conn.closed()` 会僵持到空闲超时（30 秒），
   功能对但慢得没有道理。改成主动关闭、不等。
5. **一个坏客户端能让主机停止服务**：对端中途退出导致的握手失败如果冒泡，
   主机就再也不接受连接了。握手失败必须被容忍。
6. **身份文件并发竞态**：证书与私钥分两个文件、两次 rename 发布，
   多进程同时启动会交叉发布出"证书 A + 私钥 B"，报 `KeyMismatch`。
   改为单文件承载身份，一次 rename 完成原子发布。
7. **渲染线程死锁**：进度渲染线程等通道关闭，而 sender 还活在栈上，
   表现为"传完了却卡住不退出"。收尾必须先 drop sender。
8. **续传要能用，主机必须能重连**：主机只 accept 一次的话，
   接收端断线后重连没人接——续传在真实使用中等于不存在。

## 与规划的两处偏离（都是刻意的）

| 规划 | 实际 | 原因 |
|---|---|---|
| 传输用 QUIC + 多流并行 | QUIC，但 v1 用**单条双向流**跑完整会话 | 先保证正确性；多流并行协议已预留，v1.5 再做 |
| 空闲超时用 QUIC 默认值 | 显式设为 12 秒 | 接收端被强杀不会发送关闭帧，默认 30 秒意味着主机半分钟不能接新连接 |

## 下一步

**M6：最小可用 UI（Tauri v2）** —— 内核已经能跑，但只有终端界面。
需要做的三个界面：主窗口（拖拽区 + 二维码 + 倒计时）、传输中（进度/速度/剩余）、
结果（成功/失败 + 可操作提示）。

内核已经为 UI 准备好：`ProgressEvent` 是一套与 UI 无关的进度模型，
Tauri 只需要订阅同一批事件换一种渲染方式，不需要改内核。

**尚未开始**：mDNS 自动发现、TCP 回退路径、移动端、一键网络诊断。
（网络诊断值得早做：AP 隔离和企业防火墙是"发现失败 = 产品失败"的头号原因，
而且代码解决不了，只能给出可操作的排障指引。）
