# Strange Room 产品方向决策

> **状态**：草案，核心假设待验证实验确认（见第八章）
> **日期**：2026-09-10
> **决策范围**：是否继续将 Strange Room 发展为"跨平台局域网多功能协作平台"，以及如果做，做什么、怎么做

---

## 一、决策摘要

**决定：放弃"多功能协作平台"这一定位，转向一个锐利的单一场景产品。**

新定位一句话：**让物理上同处一室的人，能像把设备摊在同一张桌子上一样共享东西。**

三个不可替代的支柱：

1. **零准备** —— 双击打开，10 秒内得到一个可用空间，无账号、无邀请流程
2. **快得离谱** —— 局域网直连，1GB 文件是秒级而不是分钟级
3. **不留痕** —— 房间关闭即销毁，端到端加密，不落云

同时决定：

- **技术栈整体推翻重建**，不沿用 Next.js + Electron 方案。改用 Tauri v2 + QUIC + mDNS 的原生客户端架构（理由见第五章）。
- **不使用浏览器作为主要客户端形态**。存在已验证的架构级硬约束使其不可行（见 2.2）。
- **构建之前先做验证实验**。本产品的核心假设尚未被验证，MVP 开发排在实验之后（见第八章）。

---

## 二、背景：现状诊断

### 2.1 项目实际架构

| 项目 | 实际实现 |
|---|---|
| 前端 | Next.js 14 App Router |
| 服务端 | 独立 Node.js `ws` 服务，端口 9001 |
| 协同同步 | y-websocket，**经服务器星型中转** |
| 文件传输 | **未实现** |
| 桌面端 | Electron 壳，生产模式加载外部 `localhost:3000` |
| 数据持久化 | 服务器内存 Map，重启即丢 |

规模约 8800 行，37 次提交（2025-06 至 2026-02），无测试、无 CI。

### 2.2 关键发现

**（1）README 描述的架构与代码不一致**

README 宣称 P2P 直连、"数据不经过中转服务器"、服务器无法窥探内容。实际代码并非如此：

- 所有协同数据都经服务器转发。客户端使用 `WebsocketProvider`（`src/lib/yjs/y-doc.ts:113`），服务器将二进制消息原样广播给同房间客户端（`server/signaling-server.js:554-576`）。这是星型中转，不是点对点。
- **WebRTC 完全没有用起来**。`y-webrtc`、`peerjs` 在 `package.json` 中，但 `src/` 内不存在任何 `RTCPeerConnection` / `RTCDataChannel` 的实际调用。服务器预留了 `/signaling` 通道，客户端未接入。
- **端到端加密只覆盖聊天文本**。仅 `sendEncryptedChatMessage`（`src/lib/yjs/y-doc.ts:478`）加密消息内容；白板和代码的 Yjs 更新明文经服务器转发，服务器完全可读。
- **密钥放在 URL 查询串中**（`src/app/room/[id]/page.tsx:543`），会进入服务器日志和浏览器历史。

**（2）已验证的架构级硬约束（决定技术选型的关键）**

`src/lib/crypto/e2e-crypto.ts` 直接调用 `crypto.subtle`（第 16、41、64、96 行），**没有任何安全上下文检测，也没有降级方案**。

而浏览器只在安全上下文（HTTPS 或 localhost）下提供 `crypto.subtle`。这意味着同事通过 `http://192.168.1.x:3000` 访问时，`crypto.subtle` 为 `undefined`，`generateRoomKey()` 直接抛异常——**当前项目的端到端加密在局域网环境下是失效的**。同理 `RTCPeerConnection` 也不可用。

这不是一个待修的 bug，而是一个架构结论：**"浏览器 + 局域网 HTTP" 这条路有天花板。** 绕开它需要给局域网配受信任的 HTTPS，而：

- 自签证书 → 访客需手动信任，体验差
- 公网证书解析到内网 IP → 会被浏览器的 DNS rebinding 保护拦截

**因此：放弃浏览器优先，走原生客户端。**

**（3）其他事实**

- 文件传输是空的。`filesMap` 与 `FileTransfer` 类型已定义（`src/types/room.ts`、`src/lib/yjs/y-doc.ts:53`）但无实现；聊天仅支持图片，走 base64 塞进 Yjs 数组，硬上限 5MB（`src/components/Chat/ChatBox.tsx:89-111`）。
- Electron 生产模式直接 `loadURL('http://localhost:3000')`（`electron/main.js:41`），依赖外部已启动的 Next 服务；`next.config.js` 无 `output: 'export'`；`allowedOrigins` 硬编码了 `talk.blksword.com`（`electron/main.js:53-56`）。**当前不存在可分发的独立桌面应用。**
- 房间状态存于进程内存（`server/signaling-server.js:19`），无数据库。
- WS 地址由页面 hostname 拼接（`src/lib/yjs/y-doc.ts:91-100`），这一点对局域网友好，但端口写死 9001，无设备发现，需手输 IP。

### 2.3 竞争现实

"局域网协作/传输"是被反复实现过的品类，单点功能均有成熟且维护活跃的开源替代：

| 场景 | 现有方案 |
|---|---|
| 局域网文件直传 | LocalSend、Snapdrop / PairDrop、croc、Magic Wormhole |
| 设备互联 | KDE Connect、Syncthing |
| 协作套件 | Jitsi Meet、Nextcloud、CryptPad / HedgeDoc / Etherpad |
| 白板与代码协同 | Excalidraw、tldraw、VS Code Live Share |
| 中文圈 | 飞秋（IPMsg）、内网通 |

**结论：不做"再实现一遍"，只做"融合 + 临时性"这两件现有方案都不做的事。**

---

## 三、产品决策

### 3.1 定位

**一个"同一张桌子"式的协作空间——因为人就在旁边，不需要经过云端。**

背后的观察：远程协作工具（Zoom / Miro / FigJam / Slack）的体验已被卷到极致，但它们都是为**分布式团队**设计的。人们其实大量时间是**物理上同处一室**的，而这时想快速共享点东西，体验反而退化回了 U 盘、微信群传文件、扫码加好友。唯一做对这个场景的是 AirDrop，但它锁死在苹果生态。

### 3.2 三个支柱（按护城河深度排序）

1. **零准备** —— 一台机器双击打开，屏幕出现二维码，10 秒内得到可用空间。没有账号、没有建团队、没有邀请邮件。访客扫码即入。
2. **快得离谱** —— 同一 WiFi 下传 1GB 是秒级而非分钟级。这是局域网相对云端的物理优势，是云厂商无法用钱解决的差距。
3. **不留痕** —— 房间关闭，数据即不存在。端到端加密，不落云。这既是信任卖点，也是最难被云厂商抄袭的一条：他们的商业模式就是要留存数据。

### 3.3 为什么"多功能平台"是错误方向

云厂商的护城河在**整合**与**生态**，这正是独立开发者最不该选的战场：

- **功能堆叠 = 红海正面竞争**。视频会议打不过 Zoom，白板打不过 Figma/Miro，聊天打不过 Slack/微信。
- **多会稀释唯一的差异化**。"临时、用完即焚"是当前唯一独特的东西，铺功能会直接把它稀释成"又一个全都做一点但都不好"的工具。
- **"整合体验"不是价值主张，是结果**。用户不会因为"你整合了五个功能"而使用，只会因为"我不用再开五个工具、注册五个账号"而使用。前者是自嗨，后者才是需求。

"融合"必须作为**结果**出现，而不是作为卖点被宣传。

---

## 四、目标用户与推进节奏

三个阶段对应不同打法，不要跳级：

**阶段一 · 起步楔子：线下工作坊 / 会议场景中的技术与创意团队**
- 特征：自传播、无需销售、容忍粗糙、热爱无账号工具
- 作用：把产品带进各自的公司

**阶段二 · 最高痛点细分：现场媒体制作**（摄影、摄像、直播、片场）
- 痛点：现场文件巨大、网络经常不可用、需立刻交付客户或 DIT、所有人就在同一房间
- 现状方案：递 SD 卡，或龟速网盘
- 价值：痛点急、反复发生、**愿意付钱**

**阶段三 · 长期现金牛：离线 / 内网环境**（工厂产线、船舶、实验室、医疗、涉密单位）
- 价值主张：云厂商在架构上无法服务，没有竞争
- 前置条件：销售能力、合规、审计功能——单人开发不宜作为起点

> **资源分叉**：单人全职 → 死磕阶段一、二；已有团队与资金 → 可直接考虑阶段三。

---

## 五、技术架构决策

### 5.1 技术选型

| 层面 | 选型 | 理由 |
|---|---|---|
| 客户端框架 | **Tauri v2**（非 Electron） | 体积 5-10MB vs 150MB，直接影响"双击即用"体验；Rust 内核可做原始 socket / QUIC / mDNS，Electron 做不到；覆盖移动端 |
| 设备发现 | **mDNS (`mdns-sd`) + UDP 广播** | "打开就看到旁边设备"的唯一实现路径，只有原生能做 |
| 文件传输 | **QUIC (`quinn`) over TLS 1.3** | 多路复用（多文件并行无队头阻塞）、加密内建、丢包 WiFi 下表现更优；相对 LocalSend 的 HTTP/TCP 是技术优势 |
| 传输安全 | **自签证书 + 指纹固定，指纹编码进二维码** | 无需 CA、无需账号、无需云端；安全性等价于 Signal 安全码比对 |
| 协同同步 | **Yjs**（备选 Automerge） | 白板 / 笔记的 CRDT 同步，生态更成熟 |
| 大文件处理 | Rust 流式读写 | 禁止在内存中缓冲整个文件 |

### 5.2 核心设计：二维码信任模型

**二维码同时承载三件事：地址 + 房间密钥 + 证书指纹。**

一次扫码即完成发现、邀请与互信。这是整个产品最关键的设计点：

- 无账号体系
- 无 CA 依赖
- 无云端参与
- 信任锚点通过带外信道（屏幕 → 摄像头）传递，等同于 Signal 的安全码比对

这个原语同时解决了"怎么找到对方"和"怎么信任对方"两个问题，是产品"魔法感"与安全性能够同时成立的唯一原因。

### 5.3 现有代码的去留

| 保留 | 理由 |
|---|---|
| Yjs 数据模型与 CRDT 结构设计 | 与选型一致，可迁移 |
| 房间生命周期 / 空闲超时 / 自动销毁的产品逻辑 | 是"不留痕"支柱的雏形 |
| 令牌与邀请的产品思路 | 可重新实现为二维码模型 |

| 弃用 | 理由 |
|---|---|
| Next.js / Electron 架构 | 与原生客户端方案冲突 |
| y-websocket 星型中转 | 与"局域网直连"核心价值冲突 |
| 浏览器端 `crypto.subtle` E2E 路径 | 在局域网安全上下文中不可用（见 2.2） |
| 白板 Canvas / Monaco 前端实现 | 需在新栈上重写 |

---

## 六、商业模式

- **核心开源**。此品类的信任本身就是产品，闭源会直接杀死它。
- **个人版免费**，限制房间人数与时长。
- **Pro 一次性买断**：无限房间、屏幕共享、更大房间。
- **企业版**：自托管授权 + 策略控制 + 审计日志 + 白标 + SLA。这是离线 / 内网市场的收入来源。

---

## 七、不做清单（反目标）

明确不做，用以抵抗功能蔓延：

- ❌ 账号体系
- ❌ 云盘 / 服务端持久化（会直接摧毁"不留痕"定位）
- ❌ 通用 IM
- ❌ 项目管理
- ❌ 公网远程协作（云厂商已赢，进去就是价格战）
- ❌ v1 的音视频会议

---

## 八、验证优先：构建前的实验

**核心假设尚未验证。在写第一行代码之前必须先验证。**

**实验设计**：找一次真实的线下工作坊（10 人左右，非技术用户为主）。不使用本产品，而是自己用 LocalSend + 白板工具 + 微信群**手工组装**出目标体验，然后观察。

**唯一要观察的问题**：他们会不会说"这直接用微信传不就完了"。

| 观察结果 | 结论 |
|---|---|
| "其实不需要，我们微信群就够了" | **产品不成立**，省下六个月的开发 |
| "要是能一步搞定就好了" | 假设成立，进入 MVP |

这一步的价值极高：它用几天的成本，验证或否决整个方向。

---

## 九、MVP 范围与验收标准

**范围（4-6 周）**

1. 房间 = 一个跑在主机上的进程，显示二维码
2. 免安装单文件客户端，扫码加入
3. 文件 / 文件夹直传，带进度与断点续传
4. 共享剪贴板
5. 共享笔记

屏幕共享明确排到 v2。

**验收标准**

> 一个非技术用户，60 秒内把一个 2GB 的文件夹传给同房间的人，全程无需任何解释。

达不到这条，就还没有产品。

---

## 十、风险

| 风险 | 说明 |
|---|---|
| 单点被免费方案覆盖 | LocalSend 免费开源且已赢下"传文件"。**因此不能以传文件为卖点，必须以"空间"为卖点，传文件只是其中一项。** |
| "为什么不拼三个免费工具" | 必须回答清楚。答案只能是：一次扫码全都有了（无缝）+ 不用建账号不用清理（临时）。若答不上来，产品不成立。 |
| 网络环境不可控 | AP 隔离、企业防火墙会伤害首次体验，且代码无法解决。需要内建一键网络诊断。 |
| 首次体验脆弱 | 发现失败 = 产品失败。发现机制必须做到零失败，或给出可操作的排障指引。 |
| 定位被稀释 | 最大风险来自内部：功能蔓延会把"临时、用完即焚"这个唯一差异化稀释掉。第七章的"不做清单"是对策。 |

---

## 十一、待定事项

- [ ] 完成第八章的验证实验，确认核心假设
- [ ] 确定产品命名与品牌方向（当前 "Strange Room" 语义偏"房间"，与新定位"同一张桌子"的契合度待评估）
- [ ] 确定首要目标平台（Windows 优先？还是 macOS 优先？影响 mDNS 与防火墙适配工作量）
- [ ] Pro / 企业版的功能边界划分
- [ ] 是否需要保留 Web 端的降级只读形态（如"仅查看"的访客）

---

## 附：本文档的依据来源

本决策基于对现有代码库的实际通读，而非 README 描述。关键证据位置：

- `src/lib/yjs/y-doc.ts:113` —— 使用 `WebsocketProvider`，证实为服务器中转而非 P2P
- `server/signaling-server.js:554-576` —— 服务器向房间广播二进制消息
- `src/lib/yjs/y-doc.ts:478` —— 仅聊天消息加密，白板/代码为明文
- `src/app/room/[id]/page.tsx:543` —— 加密密钥置于 URL 查询串
- `src/lib/crypto/e2e-crypto.ts:16,41,64,96` —— 无安全上下文防护，局域网下失效
- `src/components/Chat/ChatBox.tsx:89-111` —— 图片 base64 传输，5MB 上限
- `electron/main.js:41,53-56` —— 生产模式依赖外部服务，硬编码域名
- `server/signaling-server.js:19` —— 房间状态存于内存
